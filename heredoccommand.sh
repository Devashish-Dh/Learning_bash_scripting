#! /bin/bash

#now we will see what the heredocdelimiter is which is used to interact with the shell

cat << kreative # this is a var type deal, the name is a var

hello this is the creative text, 
this  :                 cat << __var_name__
is used to write some stuff to the terminal whenever the script is run but it acts as a comment

to end the info simply type in the delimiter at any later point to act as a breakpoint.

kreative
